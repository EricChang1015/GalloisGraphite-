upstream supabase_kong {
    zone supabase_kong 64k;
    server supabase-kong:8000 max_fails=0;
    keepalive 32;
}

upstream supabase_studio {
    zone supabase_studio 64k;
    server supabase-studio:3000 max_fails=0;
    keepalive 8;
}
