---
description: Mada Graphite project — overall conventions
alwaysApply: true
---

This is the Mada Graphite B2B trading platform. The single source of truth for
project-wide rules is the file [`.cursorrules`](mdc:.cursorrules) at the
repository root. **Read it before writing or refactoring any code.**

Quick reminders that override training data:

- This project uses **Next.js 16** (App Router, Server Actions, route types).
  APIs may differ from older Next.js versions; see `node_modules/next/dist/docs/`
  if uncertain.
- This project uses **Tailwind CSS v4** (CSS-variable theming, no `tailwind.config.js`).
- shadcn style: **base-nova** with **`@base-ui/react`** primitives (NOT Radix UI
  by default; only specific components like `@radix-ui/react-slot` are
  installed where needed).
- Mutations live in `src/actions/*` as Server Actions. New API routes are only
  for streaming (`/api/chat`) or webhooks.
- Database access: `@supabase/ssr` clients in `src/lib/supabase/`. Client/server
  separation is critical — `SUPABASE_SERVICE_ROLE_KEY` must never reach the
  browser.

## Dev runtime errors — do NOT rely on the overlay badge

The Next.js dev floating error badge **resets on navigation** and is **not visible**
to the agent. Use persistent sources instead:

1. **After browser testing or before declaring "done"**, run:
   ```powershell
   npm run qa:check-dev
   ```
   This reads `.next/dev/logs/next-development.log` (JSON, accumulates across pages)
   and writes a human summary to `dev-errors.latest.txt`. Exit code 1 = real ERRORs
   remain — fix them before finishing.
2. With **cursor-ide-browser** MCP: after every navigation call `browser_console_messages`.
3. Hydration warnings: prove with `node scripts/probe-ssr.mjs http://localhost:3000/<path>`.
4. `npm run build` alone is **not sufficient** for runtime / hydration issues.

Full checklist: [`.cursor/rules/verify-before-commit.mdc`](mdc:.cursor/rules/verify-before-commit.mdc).

## Implementation flow
1. Plan first, write code second.
2. Validate inputs with `zod`.
3. Return `{ data, error }` from server actions; never throw to the UI.
4. Append timeline events for every order state transition.
5. Append an `audit_logs` row for every admin action.

## Agent skills (workflows in `.cursor/skills/`)

This repo ships curated skill workflows under `.cursor/skills/`. Pick one when the task matches:

| Task | Skill |
|---|---|
| Verifying / pre-commit | [graphite-qa](mdc:.cursor/skills/graphite-qa/SKILL.md) |
| Multi-file feature | [incremental-implementation](mdc:.cursor/skills/incremental-implementation/SKILL.md) |
| Next.js 16 / Tailwind v4 / shadcn base-nova patterns | [source-driven-development](mdc:.cursor/skills/source-driven-development/SKILL.md) |
| UI change verified in browser | [browser-testing-with-devtools](mdc:.cursor/skills/browser-testing-with-devtools/SKILL.md) |
| Bug fix / Prove-It assertion | [test-driven-development](mdc:.cursor/skills/test-driven-development/SKILL.md) |
| Build / hydration / RLS / migration errors | [debugging-and-error-recovery](mdc:.cursor/skills/debugging-and-error-recovery/SKILL.md) |
| Pre-merge review | [code-review-and-quality](mdc:.cursor/skills/code-review-and-quality/SKILL.md) |
| Auth / RLS / payments / uploads / KYC | [security-and-hardening](mdc:.cursor/skills/security-and-hardening/SKILL.md) |

Full catalog: [.cursor/skills/README.md](mdc:.cursor/skills/README.md). Skills always reference real `npm run qa:*` commands — never invent new test commands.
