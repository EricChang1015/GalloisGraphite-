// Kept for backwards compatibility — the canonical component now lives in
// `components/order/PaymentVerifyActions.tsx` because both admin and seller
// use the same verify/reject UI.
export { PaymentVerifyActions } from "@/components/order/PaymentVerifyActions";
